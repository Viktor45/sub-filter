[EN](FILTER_RULES_en.md) / [RU](FILTER_RULES_ru.md)  / [ZH](FILTER_RULES_zh.md) 

# Правила проверки прокси (`rules.yaml`)

Файл `rules.yaml` помогает настраивать, **какие прокси разрешать, а какие — нет**, без пересборки программы.  
Все правила — что обязательно указывать, что запрещено, и какие параметры зависят друг от друга — теперь **прописываются только в этом файле**.

> ⚠️ **Важно**: если вы не напишете правила для какого-то протокола, программа будет использовать **«пустой» проверяльщик**, который **разрешает всё**.  
> Но базовые вещи — например, правильный хост, порт, UUID или пароль — всё равно проверяются **внутри программы** и отключить их нельзя.

---

## 🧩 Как устроен файл

```yaml
<название протокола>:
  required_params: [параметры, которые обязательно должны быть]
  allowed_values:
    <параметр>: [какие значения разрешены]
  forbidden_values:
    <параметр>: [какие значения запрещены]
  conditional:
    - when: { <параметр>: <значение> }
      require: [параметры, которые становятся обязательными при этом условии]
```

---

## ✅ Поддерживаемые протоколы

### `vless`
- **Обязательно**: `encryption` и `sni`
- **Запрещено**: `security=none`
- **Разрешено**: `security=tls` или `security=reality`
- **Потоки (`flow`)**: работают **только** если `security=reality`
- **Особые условия**:
  - Если `security=reality` → обязательно поле `pbk`
  - Если `type=grpc` → обязательно `serviceName`
  - Если `type=ws`, `httpupgrade` или `xhttp` → обязательно `path`

> 💡 Поле `encryption` осталось для совместимости с старыми настройками, хотя сейчас его часто не пишут.

### `vmess`
- **Обязательно**: `tls=tls`
- Если `net=grpc` → обязательно `serviceName`
- Остальное (адрес, ID, порт) проверяется программой автоматически

### `hysteria2`
- **Обязательно**: `obfs` и `obfs-password`
- **Разрешено только**: `obfs: salamander`
- Так сделано специально для публичных подписок

### `trojan`
- Если `type=grpc` → обязательно `serviceName`
- Пароль и адрес проверяются программой

### `ss` (Shadowsocks)
- Правил **нет** (`{}`), потому что в Shadowsocks **нет параметров в ссылке**
- Вся проверка (шифр, пароль, хост, порт) делается внутри программы

---

## 🔧 Примеры

### Разрешить VLESS без `encryption` (для старых подписок)

```yaml
vless:
  required_params: [sni]  # encryption больше не обязателен
  allowed_values:
    security: ["tls", "reality"]
```

### Разрешить Hysteria2 с `obfs=none` (для домашней сети)

```yaml
hysteria2:
  required_params: [obfs]  # obfs-password не нужен, если obfs=none
  allowed_values:
    obfs: ["salamander", "none"]
```

> ⚠️ Если вы разрешаете `obfs=none`, убедитесь, что ваша программа **не требует** `obfs-password` — сейчас это проверяется **только через правила**, поэтому такая настройка сработает.

---

## 📂 Как использовать

1. Создайте файл `./config/rules.yaml` (пример лежит в папке `./config/`)
2. Укажите его в основном конфиге:
   ```yaml
   rules_file: "./config/rules.yaml"
   ```
3. Запустите программу с флагом: `--config config.yaml`

> Если вы не укажете `rules.yaml`, программа использует **встроенные правила**, но они могут работать **не так, как вы ожидаете**.  
> **Рекомендуется всегда писать свой `rules.yaml`**, чтобы точно управлять поведением.

---